#pragma once

namespace Constants {
    inline constexpr int SCREEN_WIDTH  = 800;
    inline constexpr int SCREEN_HEIGHT = 450;
    inline constexpr int TARGET_FPS    = 60;

    inline constexpr float GRAVITY        = 200.0f; // pixels/sec^2
    inline constexpr float PLAYER_RADIUS  = 40.0f;

    inline constexpr bool DEBUG = true;  
    inline constexpr int DEBUG_TEXT_SIZE = 10;
}